import 'package:flutter/material.dart';

import 'material_color.dart';

final primaryColor =
    generateMaterialColor(generateMaterialColor(const Color(0xFF0B113A)));

final accentColor = generateMaterialColor(const Color(0xFF3A488E));

final inputColor =
    generateMaterialColor(const Color.fromARGB(255, 231, 230, 230));

const scaffoldColor = Color(0xFFEDEDF4);
const cardColor = Color(0xFFF9F9F9);

final beigeApp = generateMaterialColor(const Color(0xFFFBF1E7));
final orangeApp = generateMaterialColor(const Color(0xffF9BD5D));
final darkGreyApp = generateMaterialColor(const Color(0xff666666));
final greyApp = Colors.grey[300];
final goldApp = generateMaterialColor(const Color(0xffC59639));

final greenApp = generateMaterialColor(const Color(0xff559A4D));
final orangeDeficientApp = generateMaterialColor(const Color(0xffFF712A));
